name: Git Checker

on: [push, pull_request]

jobs:
  git-checker:
    runs-on: ubuntu-latest
    env:
      path: git-majonez
    steps:
    - name: Check out code
      uses: actions/checkout@master
    - name: Set the repo address
      run: |
        echo -n "repo=" >> $GITHUB_ENV
        cat address.txt >> $GITHUB_ENV
    - name: Checkout git-majonez repo
      uses: actions/checkout@v2
      with:
        fetch-depth: 0
        ref: master
        repository: ${{ env.repo }}
        path: ${{ env.path }}
      run: |
        git checkout master
        git log --oneline
        git log --oneline > loggg.txt
    - name: Run tree-check.sh
      working-directory: .github/scripts
      run: ./tree-check.sh ../../${{ env.path }}
    - name: Run history-check.sh
      working-directory: .github/scripts
      run: ./history-check.sh ../../${{ env.path }}/history.txt
